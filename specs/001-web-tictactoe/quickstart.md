# 快速開始指南：網頁版井字遊戲

**Phase**: 1 - 快速開始  
**Date**: 2025-11-12  
**Target Audience**: 開發者 & 使用者

---

## 目錄

1. [用戶快速開始](#用戶快速開始)
2. [開發環境設置](#開發環境設置)
3. [本地運行](#本地運行)
4. [遊戲玩法](#遊戲玩法)
5. [開發工作流](#開發工作流)
6. [常見問題](#常見問題)

---

## 用戶快速開始

### 要求

- 現代網頁瀏覽器 (Chrome, Firefox, Safari, Edge 最新版本)
- 無需安裝任何軟件

### 打開遊戲

1. 在瀏覽器中打開 `index.html` (或訪問應用URL)
2. 頁面加載後，遊戲棋盤應立即可見
3. 選擇難度 (默認"中等")
4. 點擊棋盤上的任意空位開始遊戲

### 基本操作

| 操作 | 說明 |
|------|------|
| **點擊空位** | 放置您的棋子 (X) |
| **等待 AI** | AI 自動計算著法 (最多2秒) |
| **查看結果** | 遊戲結束時顯示勝負或平局 |
| **點擊「重新開始」** | 開始新遊戲 (保持難度) |
| **更改難度** | 點擊難度選擇器，選擇新難度，重新開始遊戲 |

### 難度說明

- **簡單 (Easy)**: AI 經常犯錯，適合初學者
- **中等 (Medium)**: AI 有競爭力，均衡遊戲體驗
- **困難 (Hard)**: AI 最優演奏，經常平局或勝利

---

## 開發環境設置

### 要求

- **Node.js** 18 或更高版本 ([下載](https://nodejs.org/))
- **npm** 或 **yarn** 包管理器
- **代碼編輯器** (VS Code 推薦)
- **Git** 版本控制

### 安裝依賴

```bash
# 進入專案根目錄
cd /path/to/TicTacToe

# 安裝開發依賴
npm install

# 或使用 yarn
yarn install
```

### 安裝的工具

| 工具 | 用途 |
|------|------|
| **webpack** | 模組打包 & 開發伺服器 |
| **Babel** | ES6 轉譯 |
| **Jest** | 單位測試框架 |
| **Cypress** | E2E 測試框架 |
| **ESLint** | 代碼檢查 |
| **Prettier** | 代碼格式化 |

### package.json 腳本

```json
{
  "scripts": {
    "start": "webpack serve --mode development",
    "build": "webpack --mode production",
    "test": "jest",
    "test:watch": "jest --watch",
    "e2e": "cypress open",
    "lint": "eslint src/",
    "format": "prettier --write src/"
  }
}
```

---

## 本地運行

### 開發模式 (帶熱重載)

```bash
npm start

# 輸出:
# > webpack serve --mode development
# webpack 5.x.x compiled with warnings in XXms
# <i> [webpack-dev-server] Server running at http://localhost:8080/
```

然後在瀏覽器中打開 `http://localhost:8080`

**功能**:
- 自動重載 (修改代碼後頁面自動刷新)
- Source maps (便於調試)
- 詳細錯誤報告

### 生產構建

```bash
npm run build

# 輸出:
# dist/
# ├── index.html
# ├── bundle.js (最小化)
# └── styles.css
```

生產版本優化包括：
- JavaScript 最小化
- CSS 最小化
- 樹搖優化 (tree shaking)
- 檔案約 50KB 總大小

### 直接在瀏覽器打開

```bash
# 使用簡單HTTP伺服器 (Python 3)
python -m http.server 8000

# 或使用 Node.js
npx http-server

# 然後訪問 http://localhost:8000 或 http://localhost:8080
```

---

## 遊戲玩法

### UI 組件佈局

```
┌─────────────────────────────────┐
│   難度選擇: [簡單] [中等] [困難]  │
│                                 │
│  ┌─────┬─────┬─────┐           │
│  │  1  │  2  │  3  │           │
│  ├─────┼─────┼─────┤           │
│  │  4  │  5  │  6  │           │
│  ├─────┼─────┼─────┤           │
│  │  7  │  8  │  9  │           │
│  └─────┴─────┴─────┘           │
│                                 │
│  結果: 您勝利！ / AI勝利 / 平局  │
│                                 │
│  [重新開始]  [更改難度]         │
└─────────────────────────────────┘
```

### 遊戲流程

1. **初始狀態**: 空棋盤，玩家 (X) 先手
2. **玩家著法**: 點擊空位放置 X
   - 著法立即反映 (< 300ms)
   - 棋盤自動禁用，等待 AI
3. **AI著法**: 自動計算並放置 O
   - 計算時間 50-100ms (實際)
   - 展示時間 500ms-2秒 (帶思考延遲)
4. **勝負檢查**: 每著法後自動檢查
   - 玩家連線3個 X → 玩家勝利
   - AI 連線3個 O → AI 勝利
   - 棋盤滿 & 無人連線 → 平局
5. **結果顯示**: 清晰的勝負訊息 + 重新開始按鈕

### 勝利條件

井字遊戲勝利條件：同一玩家在以下任一位置連線3個棋子：
- **行**: 第1/2/3行
- **列**: 第1/2/3列
- **對角線**: 左上→右下 / 右上→左下

```
勝利範例 (玩家 X):

X | X | X       X | O | O       X | O | X
---------       ---------       ---------
O | O | X   或  X | O | O   或  O | X | X
---------       ---------       ---------
X | O | O       X | X | X       O | O | X
```

---

## 開發工作流

### 目錄結構

```
TicTacToe/
├── specs/001-web-tictactoe/
│   ├── spec.md (功能規格書)
│   ├── plan.md (本實裝計劃)
│   ├── research.md (Phase 0 研究)
│   ├── data-model.md (Phase 1 資料模型)
│   ├── quickstart.md (本文件)
│   └── contracts/ (API契約)
├── src/
│   ├── index.html (主HTML)
│   ├── styles/
│   │   └── game.css
│   └── scripts/
│       ├── game-board.js
│       ├── game-state.js
│       ├── ai-engine.js
│       ├── difficulty.js
│       ├── ui-controller.js
│       └── main.js
├── tests/
│   ├── unit/ (單位測試)
│   ├── integration/ (集成測試)
│   └── e2e/ (E2E 測試)
├── dist/ (生產構建輸出)
├── package.json
├── webpack.config.js
└── jest.config.js
```

### 常見開發任務

#### 修改遊戲邏輯

1. 編輯 `src/scripts/ai-engine.js` 或 `src/scripts/game-state.js`
2. 保存檔案 → 瀏覽器自動重載
3. 在 `tests/unit/` 中添加相應測試
4. 運行 `npm test` 驗證

#### 修改 UI 樣式

1. 編輯 `src/styles/game.css`
2. 保存 → 瀏覽器實時更新
3. 使用 Chrome DevTools (F12) 調試佈局

#### 添加新功能

1. 在 `specs/001-web-tictactoe/spec.md` 中記錄需求
2. 在 `data-model.md` 中定義資料模型
3. 在 `contracts/README.md` 中定義API
4. 實裝並編寫測試
5. 測試通過後，提交 Pull Request

### 測試工作流

#### 單位測試

```bash
# 運行所有測試
npm test

# 輸出示例:
# PASS tests/unit/game-board.test.js
#   GameBoard
#     ✓ should initialize empty board (5ms)
#     ✓ should set and get cell value (2ms)
#     ...
# 
# Test Suites: 4 passed, 4 total
# Tests: 32 passed, 32 total
```

#### 監看模式 (開發時有用)

```bash
npm run test:watch

# 保持運行，每當源碼變更時重新運行相關測試
```

#### E2E 測試

```bash
npm run e2e

# 打開 Cypress 測試運行器
# 選擇測試並查看實際遊戲運行
```

### 代碼風格

#### ESLint 檢查

```bash
npm run lint

# 報告代碼問題
```

#### 自動格式化

```bash
npm run format

# 使用 Prettier 自動格式化所有代碼
```

---

## 常見問題

### Q: 遊戲怎樣開始？

**A**: 打開 `index.html` 或訪問應用 URL。頁面加載後，棋盤和難度選擇器立即可見。選擇難度（默認"中等"），然後點擊棋盤上的任意空位開始遊戲。

### Q: AI 為什麼要花2秒才著法？

**A**: 這是設計特性。AI 在 50-100ms 內計算著法，但額外延遲 (500ms-2秒) 是為了：
1. 模擬人類思考時間，讓遊戲體驗更自然
2. 避免顯得 AI 反應太快而不合理

### Q: 我可以保存遊戲進度嗎？

**A**: 目前不支持遊戲進度持久化。刷新頁面後，遊戲重置。若要添加此功能，可使用 LocalStorage API。

### Q: 為什麼困難模式下很難贏？

**A**: 因為困難模式使用 Minimax 算法的最優演奏。在井字遊戲中，如果 AI 使用最優演奏，玩家最佳結果是平局。若玩家犯錯，AI 會立即懲罰並贏得遊戲。

### Q: 我可以在手機上玩嗎？

**A**: 可以。應用在手機瀏覽器上可使用，但未針對手機優化。觸摸響應可能不如桌面順暢。

### Q: 代碼由誰維護？

**A**: 本應用由 TicTacToe 專案團隊維護。所有變更應通過規格 (spec.md) 與設計文件 (data-model.md) 記錄。

### Q: 我在運行測試時遇到錯誤。怎樣解決？

**A**: 
1. 確認 Node.js 版本 ≥ 18: `node --version`
2. 重新安裝依賴: `rm -rf node_modules && npm install`
3. 檢查是否有環境變數衝突 (如舊版 npm)
4. 查看 Jest 輸出中的詳細錯誤訊息

### Q: 如何在無網際網路的情況下遊玩？

**A**: 應用完全離線運行。下載所有檔案後，在本地用 HTTP 伺服器打開 (見[本地運行](#本地運行)部分)，無需網際網路連接。

---

## 後續步驟

### 對於玩家

- 享受遊戲！試試所有難度級別
- 在簡單模式下練習基本策略
- 挑戰困難模式

### 對於開發者

- 閱讀 `spec.md` 理解功能需求
- 查看 `data-model.md` 理解技術設計
- 查看 `contracts/README.md` 理解 API
- 運行 `npm start` 開始開發
- 提交變更前運行 `npm test && npm run lint`

### 相關文件

| 文件 | 用途 |
|------|------|
| [spec.md](spec.md) | 功能規格與需求 |
| [plan.md](plan.md) | 實裝計劃與架構 |
| [research.md](research.md) | 技術研究與決策 |
| [data-model.md](data-model.md) | 資料模型與實體 |
| [contracts/README.md](contracts/README.md) | API 契約 |

---

**祝您遊戲愉快！🎮**
